import 'package:flutter/material.dart';

// This widget represents the 'HomeTab' route group in AppRouter
// @RoutePage(name: 'HomeTab') // Name matches AppRouter's name: 'HomeTab'
class HomeTab extends StatelessWidget {
  const HomeTab({super.key});

  @override
  Widget build(BuildContext context) {
    // This widget typically stays empty.
    // AutoRoute handles displaying the correct child page (HomeRoute or HomeDetailsRoute).
    return  Container() ;
  }
}